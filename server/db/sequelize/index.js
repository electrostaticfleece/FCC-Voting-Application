import connect from './connect';
import controllers from './controllers'
import passport from './passport'


export { connect, passport, controllers };

export default {
  connect,
  passport,
  controllers
};